# Capability 02 feature analysis

This executed POC notebook summarizes the ten golden personas and a deterministic synthetic background sample. It retains compact tables and plots only; credentials and source extracts are excluded.

Run with cell-failure enforcement:

```bash
poetry run jupyter nbconvert --execute --to notebook --inplace notebooks/02_features/02_features.ipynb
```

The results demonstrate pipeline behavior, not production population accuracy.
